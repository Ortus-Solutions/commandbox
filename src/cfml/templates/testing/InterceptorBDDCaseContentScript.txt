			it( "should execute |point|", function(){
				// mocks
				var mockEvent = getMockRequestContext();
				var mockData  = {};
				
				// execute |point|
				interceptor.|point|( mockEvent, mockData );
				
				// expectations here
				expect( false ).toBeTrue();
			});