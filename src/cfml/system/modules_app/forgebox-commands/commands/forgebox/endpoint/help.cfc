component excludeFromHelp=true {

	function run()  {

		print.line()
			.yellow( 'The ' ).boldYellow( 'forgebox endpoint' ).yellowLine( ' namespace helps you manage different enterprise ForgeBox installs' )
			.yellowLine( 'Use these commands to list your registered ForgeBox endpoints as well as add/remove them' )
			.line()
			.yellowLine( 'Type help before any command name to get additional information on how to call that specific command.' )
			.line();

	}
}
